import assets from "../assets";

const style = {
    container: {
        flex: 1,
        alignItems: "flex-start",
        justifyContent: "flex-start",
        flexDirection: "column",
        backgroundColor: assets.colors.background.a
    }
}

export default style ;